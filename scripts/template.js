/* imports */

export const FormSchema = Object.freeze(
  /** @type {const} */ ({
    /* form -> schema */
  }),
)

export const FormKind = Object.freeze(
  /** @type {const} */ ({
    /* form -> kind */
  }),
)
